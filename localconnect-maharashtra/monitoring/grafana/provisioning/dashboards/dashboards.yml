apiVersion: 1

providers:
  - name: LocalConnect
    orgId: 1
    folder: LocalConnect
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
